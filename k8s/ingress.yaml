apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: enterprise-huxo-app-ingress
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: enterprise-huxo-app-service
            port:
              number: 80